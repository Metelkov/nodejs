2 файла - один для релиза, второй нет - он просто для истории, пусть лежит

парсер - распарсивает большой vcf файл, который содержит много vcf 
карточек - полуется после экспорта из web форм почты

требует nodejs, работает из консоли, распарсиваемый файл должен 
быть в корне, вывод тоже валится в корень

внутри, перед работой необходимо изменить имя "большого" файла
или посмотреть как он должен называться и в выводе изменить расширение
с txt на vcf иначе вывод будет в текстовые файлы

----
2 files - one for the release, the second is not - it's just for history, let it lie

parser - parsing a large vcf file that contains many vcfs
cards - obtained after export from web mail forms

requires nodejs, works from the console, the file to be parsed must
be at the root, the conclusion also falls to the root

inside, before working, you need to change the name of the "large" file
or see what it should be called and change the extension in the output
from txt to vcf otherwise the output will be in text files
